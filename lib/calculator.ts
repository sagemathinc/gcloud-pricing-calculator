/*
Use the data to calculate useful things.
*/

import { getData, PricingData } from "./get-data";

/*
Given an instance type label such as "n2-highmem-2",
this function returns the on demand and spot prices
for that instance in every region in the world.
Here data is
*/
export function getPricing(instanceType: string, data: PricingData) {

  
}
